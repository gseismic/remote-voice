# 代码编写规范
【必须】是指 RFC-2119 中的MUST

## 配置
交接文档: docs/HANDOFF.md
代码设计文档目录: docs/design
侦察文档位置: docs/dev
计划文档和结果目录: docs/dev
review文档的位置: docs/dev
历史计划执行文档: docs/dev/INDEX.md
逆向功能规格说明书位置: docs/reverse-spec/
是否提交docs/目录: 是
是否提交AGENTS.md: 是
git commit语言: 中文
文档语言: 中文
注释语言: 中文

## 必须遵守的原则
在对话中，当对方提出一个观点、方案或提问时，你必须先自行判断其合理性，再
回应。

### 规则
- 对方说"我认为应该X"，不等于X是对的。你要独立判断X是否合理、逻辑是否自
洽、是否有更好的方案。
- 如果发现对方有错，直接指出。不要绕弯，不要先肯定再否定。
- 对方问你"是不是应该Y"，不是在暗示你做Y。如实回答，不要揣测对方期望什么
答案。
- 对方每提一个新想法，如果你的第一反应是"好，我来做"，停一下。先问自己：
这个想法解决的真实问题是什么？值不值得做？
- 不需要在每轮对话末尾问"你觉得呢"或"需要我继续吗"。问题解决完了就停。
- 如果对方追问你的设计选择，给出理由。如果理由站不住，承认并修正。

### 不允许

- 为了保持一致而放弃更好的判断
- 因为怕冲突而回避指出对方的错误
- 抢先重构对方没要求改的代码

- 你的回答（特别是输出的文档）应尽可能的可二次追踪复核，引用要包含相对路径，必要时，应包含行号。
- 回答问题前，思考用户的真实意图是什么，思考用户为什么提出这个问题。你应识别“用户问题”背后的“真实意图”。比如，用户描述一个现象时，你需要研究现象发生的原因是什么，用户为什么关注这个现象，联系最近用户的问题和结果，分析意图是什么。总之，你应充分理解了【用户意图】，并且【必须】把用户的意图明确说出来，防止用户表达错误。
- 理解和解决任何问题前，必须确保自己拥有足够的上下文信息，找到“表象”背后的真实“根因”，你的目标是解决问题，而不是解决表象。用户描述现象一般是为了描述问题。
- 除非是非修改性的问答和简单的文件改动，其他所有问题，实施前【必须】按要求拟定文化的计划
- 计划实施后，要写计划实施的结果文件，commit时，【必须】包含计划文件和结果文件的路径（相对于项目根目录）

## 如何开始
优先阅读: 
- 交接文档，了解过去的读写历史和结论。（这个文档可能过时，要根据git记录判断）
- README.md (可能过时，主要是为了了解我们的项目目标)
- 项目文件夹结构
- 根据需要，读取git log
- 历史计划执行文档(PLAN-{XXX}.md, PLAN-{XXX}-OUTCOME.md，通过这个理解项目演化和用户的意图演化、项目进度

## 如何确定下一步的工作
如果用户没有指定，应该思考本系统的最终目标是什么，当前代码状态下的高价值、最紧急的任务是什么。

## 如何写侦查文档
侦查文档一般是由低阶模型形成【预研究】文档，是为更高阶模型提供上下文参考，用于侦查的模型相当于高阶模型的秘书。
格式要求: 侦查文档一般要在开头写明依赖的commit-id，编写时间，使用的模型和版本号，研究的问题等，正文要写清楚已经查明的问题，依据是什么，要注明文件名和行号，还有哪些问题待解决等。
文件命名: {20260719: date e.g. YYYYMMDD}-{1558: HHMM}-PRE-{commit-id: git commit 前8位e.g. e7928a3}-{topic}.md

## 如何做设计
对于设计，要参考api-design-zh这个skill。特别是用户接口，要提出不同方案，比较优缺点，然后综合方案才能定稿，然后这个定稿也【必须】做1~3次查漏补缺确定。
设计命名为 {topic}-{date:YYYYMMDD}-{tag}.md 如，memory-20260606-overview.md

## 如何制定计划
- 计划文件命名为 PLAN-{XXX:数字}-{tag}.md 放在对应目录，比如命名为: PLAN-001-function.md
生成计划文件后，【必须】自查1~3遍，特别是审查是否有设计缺陷、逻辑冲突等问题

## 如何实施代码
按计划实施代码，如果计划实施前有更新，先更新计划文件再实施。实施完代码应该review一遍代码，如果有问题，应该修复，然后生成结果文件，比如PLAN-001-function-OUTCOME.md。
要在{历史计划执行文档}中维护计划和结果文件的对应(包含文件名），并做计划-执行摘要, 应该包含简单摘要，一般只追加，不删改，应该包含更新时间，精确到分钟。时间是顺序增加。
每次实施完要提交一次代码。

## 如何提交代码
- 遵守我们的配置，如果提交docs/ ，应该在commit消息中记录我们实施的计划文件和结果文件，如果有新增设计文件，应该在commit中明确给出文件位置，逐条详细记录我们的变更，确保只读commit内容就可以了解本次变动。
- commit后要立刻push。

## review文档的格式
当用户要求形成review文档时，把review意见写入文件
review后形成的文档文件命名: 20260719-1558-REVIEW-{commit-id: 前8位e.g. e7928a3}-{topic}.md

## 如何审核review文档
当用户要求你审核某个review文档时，应该在原来review文档文件名加`-review-verification`，路径名和扩展名不变，比如review文档时XXX-REVIEW-YYY.md，你生成的文档应该是 XXX-REVIEW-YYY-review-verification.md

## 如何写逆向功能规格说明书(RFS)
RFS命名: 20260719-{1558: HHMM}-RFS-{commit-id: git commit 前8位e.g. e7928a3}-{topic}.md
默认生成整个代码库的RFS，如果用户指定，则生成特定模块的RFS
- 逆向功能规格说明书 (Reverse Functional Specification, RFS)是通过阅读代码，纯从代码客观恢复的功能行为定义。 文档读起来应该像一份详细到极致的产品功能说明，但所有描述都必须有代码证据支撑。
- 写完后要复核， 验收标准是： 一个没有看过原代码的新团队，拿到这份文档和任何空白技术栈，能实现出一个在用户可感知的交互、逻辑、数据边界、异常处理上与现有系统完全一致的替代品。

## 如何写交接文档
什么时候写交接文档:
- 用户要求
- 5小时的额度不足5%
交接文档写什么:
- 交代项目背景，确保0背景的人或agent可以直接了解项目。文档中需要写: 当前做了什么工作，有哪些可能要做的事和方向，可能需要优先处理的问题。要准确描述和引用相关文档。

## 常见问题

### 当{历史计划文档}不存在
应该创建文件，如果已经有历史信息缺失，应该做项目介绍，然后如实描述之前的信息缺失，自行决定是否补充旧信息。
